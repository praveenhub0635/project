pipeline {
    agent any
    stages {
        stage('docker service'){
            steps {
                sh 'sudo service docker start'
            }
        }
        stage('docker build') {
            steps {
                sh 'docker build -t praveen0635/ang-cal .'
            }
        }
    }
}